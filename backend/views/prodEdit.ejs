<!DOCTYPE html>
<html lang="es">
<head>
    <%- include('./partials/head')%>
    
    <link rel="stylesheet" href="/static/css/prodEdit.css">
    <title>Dorid Mantenimiento</title>
</head>
<body>
    <header>
        <%- include('./partials/header')%>
    </header>

    <main class="main-edit">
        <h3 class="titulo-edit">Mantenimiento de productos</h3    >
        <h5 class="titulo-edit">Los campos marcados con * son requeridos</h5>
        <h4 class="titulo-edit">SKU Nº <%= producto.id %></h4>

        <!-- // Errores del frontend -->
        <div>
            <ul id="erroresFront" > </ul>
        </div>

        <form action="/products/edit/<%= producto.id %>?_method=PUT" method="POST" class= 'prodEditForm'>
<!--            <label for="id" class="label">SKU *</label>
                <input class="campo-input" type="text" name="id" placeholder="<%= producto.id %>" required minlength=2>-->
            <label for="name" class="label">Nombre Producto *</label>
                <input class="campo-input" type="text" name="name" value="<%= producto.nombre %>" >
            <label for="description" class="label">Descripción *</label>
                <textarea class="campo-input" name="description" ><%= producto.descripcion %></textarea>
<!--             <label for="image" class="label">Seleccione las imagenes del producto </label>
                <input class="campo-input" type="file" name="image" accept= ".jpg .png" multiple>
 -->
            <label for="category" class="label">Categoría *</label>

            <div class="categoria">
                <input class="categoria-selector" type="text" name="category" value="<%= producto.categoria %>" >
                <select name="newCategory" class="categoria-selector" >
                    <option selected>Seleccione nueva</option>
                    <option value="1">Robot fijo</option>
                    <option value="2">Robot móvil</option>
                    <option value="3">Repuesto</option>
                </select>
            </div>
            <label for="price" class="label">Precio *</label>
                <input class="campo-input" type="number" name="price" value="<%= producto.precio %>" >
            <label for="discount" class="label">Descuento *</label>
                <input class="campo-input" type="number" name="discount" value="<%= producto.descuento %>">
            

<!--           <div class="g-recaptcha" data-sitekey="your_site_key"></div>
               <input type="submit" value="Submit">-->

            <div class="botonera">
                <button class="button-registro" type="submit">Aceptar cambios</button>

            </div>
        </form>
    </main>

    <footer>
        <%- include('./partials/footer')%>
    </footer>
    <script src="/static/js/productEditValidate.js" ></script>
</body>
</html>